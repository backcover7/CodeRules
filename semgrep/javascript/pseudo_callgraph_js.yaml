rules:
  - id: pseudo_callgraph_js
    patterns:
      - pattern-either:
        - pattern-inside: function $X(...) { ... }
        - pattern-inside: $Y = function $X(...) {...}
      - pattern-either:
        - pattern: $A = $B
        - pattern: $OBJ.$CALLEE(...)
    message: $CALLER calls $OBJ.$CALLEE
    languages:
      - javascript
    severity: INFO