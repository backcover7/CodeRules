rules:
  - id: ${node} 
    options:
      symbolic_propagation: true
    pattern-either:
      - pattern: (play.twirl.api.Html$ $A).apply(...)
    message: Semgrep found a match of method in ${node} csv
    languages: [java]
    severity: WARNING